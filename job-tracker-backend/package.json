{
  "name": "the-job-log-api",
  "version": "2.0.0",
  "private": true,
  "description": "Production-ready REST API for The Job Log",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check db.js && node --check middleware/authMiddleware.js && node --check routes/auth.js && node --check routes/jobs.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
