# The Job Log — Frontend

React frontend for The Job Log.

## Local setup

```bash
cp .env.example .env
npm install
npm start
```

Set `REACT_APP_API_URL` to the backend URL. For production, Vercel should use the deployed Render API URL and be redeployed after changing the environment variable.
